{
  "name": "stock-lens",
  "version": "0.1.0",
  "private": true,
  "description": "AI stock dashboard with a local finance API proxy.",
  "scripts": {
    "start": "node server.js",
    "check": "node --check app.js && node --check server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
